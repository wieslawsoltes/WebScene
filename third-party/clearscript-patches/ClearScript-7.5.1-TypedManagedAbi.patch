diff --git a/ClearScriptV8/V8ContextImpl.cpp b/ClearScriptV8/V8ContextImpl.cpp
index e7d9c798..5d49ad92 100644
--- a/ClearScriptV8/V8ContextImpl.cpp
+++ b/ClearScriptV8/V8ContextImpl.cpp
@@ -2,6 +2,7 @@
 // Licensed under the MIT license.
 
 #include "ClearScriptV8Native.h"
+#include <limits>
 
 //-----------------------------------------------------------------------------
 // support for v8::Maybe and v8::MaybeLocal
@@ -372,6 +373,70 @@ constexpr enum class DummyCallbackStatus {} t_CallbackStatus {};
 //-----------------------------------------------------------------------------
 
 static std::atomic<size_t> s_InstanceCount(0);
+static std::atomic<V8ContextImpl::WebSceneReadDomNumericPropertyMethod*> s_pWebSceneReadDomNumericProperty(nullptr);
+static std::atomic<V8ContextImpl::WebSceneWriteDomRectMethod*> s_pWebSceneWriteDomRect(nullptr);
+
+//-----------------------------------------------------------------------------
+
+bool V8ContextImpl::RegisterWebSceneTypedManagedAbi(int32_t version, void* pvReadDomNumericProperty, void* pvWriteDomRect) noexcept
+{
+    if ((version != 1) || (pvReadDomNumericProperty == nullptr) || (pvWriteDomRect == nullptr))
+    {
+        return false;
+    }
+
+    s_pWebSceneReadDomNumericProperty.store(
+        reinterpret_cast<WebSceneReadDomNumericPropertyMethod*>(pvReadDomNumericProperty),
+        std::memory_order_release);
+    s_pWebSceneWriteDomRect.store(
+        reinterpret_cast<WebSceneWriteDomRectMethod*>(pvWriteDomRect),
+        std::memory_order_release);
+    return true;
+}
+
+//-----------------------------------------------------------------------------
+
+void V8ContextImpl::WebSceneReadDomNumericPropertyCallback(const v8::FunctionCallbackInfo<v8::Value>& info)
+{
+    auto pCallback = s_pWebSceneReadDomNumericProperty.load(std::memory_order_acquire);
+    if ((pCallback == nullptr) || (info.Length() < 2))
+    {
+        info.GetReturnValue().Set(v8::Number::New(info.GetIsolate(), std::numeric_limits<double>::quiet_NaN()));
+        return;
+    }
+
+    auto hContext = info.GetIsolate()->GetCurrentContext();
+    auto nodeId = info[0]->IntegerValue(hContext).FromMaybe(0);
+    auto property = info[1]->Int32Value(hContext).FromMaybe(-1);
+    info.GetReturnValue().Set(v8::Number::New(info.GetIsolate(), pCallback(nodeId, property)));
+}
+
+//-----------------------------------------------------------------------------
+
+void V8ContextImpl::WebSceneWriteDomRectCallback(const v8::FunctionCallbackInfo<v8::Value>& info)
+{
+    auto pCallback = s_pWebSceneWriteDomRect.load(std::memory_order_acquire);
+    if ((pCallback == nullptr) || (info.Length() < 3) || !info[2]->IsFloat64Array())
+    {
+        info.GetReturnValue().Set(false);
+        return;
+    }
+
+    auto hArray = info[2].As<v8::Float64Array>();
+    if (hArray->Length() < 8)
+    {
+        info.GetReturnValue().Set(false);
+        return;
+    }
+
+    auto hContext = info.GetIsolate()->GetCurrentContext();
+    auto nodeId = info[0]->IntegerValue(hContext).FromMaybe(0);
+    auto kind = info[1]->Int32Value(hContext).FromMaybe(-1);
+    auto spBackingStore = hArray->Buffer()->GetBackingStore();
+    auto pValues = reinterpret_cast<double*>(
+        static_cast<uint8_t*>(spBackingStore->Data()) + hArray->ByteOffset());
+    info.GetReturnValue().Set(pCallback(nodeId, kind, pValues) != 0);
+}
 
 //-----------------------------------------------------------------------------
 
@@ -461,6 +526,8 @@ V8ContextImpl::V8ContextImpl(SharedPtr<V8IsolateImpl>&& spIsolateImpl, const Std
 
             ASSERT_EVAL(FROM_MAYBE(m_hContext->Global()->Set(m_hContext, CreateString("getPromiseState"), FROM_MAYBE(v8::Function::New(m_hContext, GetPromiseStateCallback, hContextImpl)))));
             ASSERT_EVAL(FROM_MAYBE(m_hContext->Global()->Set(m_hContext, CreateString("getPromiseResult"), FROM_MAYBE(v8::Function::New(m_hContext, GetPromiseResultCallback, hContextImpl)))));
+            ASSERT_EVAL(FROM_MAYBE(m_hContext->Global()->Set(m_hContext, CreateString("__webSceneNativeReadDomNumericProperty"), FROM_MAYBE(v8::Function::New(m_hContext, WebSceneReadDomNumericPropertyCallback, hContextImpl)))));
+            ASSERT_EVAL(FROM_MAYBE(m_hContext->Global()->Set(m_hContext, CreateString("__webSceneNativeWriteDomRect"), FROM_MAYBE(v8::Function::New(m_hContext, WebSceneWriteDomRectCallback, hContextImpl)))));
 
             m_hMissingPropertyValue = CreatePersistent(CreateSymbol());
             m_hHostExceptionKey = CreatePersistent(CreateString("hostException"));
diff --git a/ClearScriptV8/V8ContextImpl.h b/ClearScriptV8/V8ContextImpl.h
index 335bfe70..90f532a3 100644
--- a/ClearScriptV8/V8ContextImpl.h
+++ b/ClearScriptV8/V8ContextImpl.h
@@ -19,9 +19,13 @@ class V8ContextImpl final: public V8Context
 
 public:
 
+    typedef MANAGED_METHOD(double) WebSceneReadDomNumericPropertyMethod(int64_t nodeId, int32_t property);
+    typedef MANAGED_METHOD(StdBool) WebSceneWriteDomRectMethod(int64_t nodeId, int32_t kind, double* pValues);
+
     V8ContextImpl(V8IsolateImpl* pIsolateImpl, const StdString& name);
     V8ContextImpl(SharedPtr<V8IsolateImpl>&& spIsolateImpl, const StdString& name, const Options& options);
     static size_t GetInstanceCount();
+    static bool RegisterWebSceneTypedManagedAbi(int32_t version, void* pvReadDomNumericProperty, void* pvWriteDomRect) noexcept;
 
     const StdString& GetName() const { return m_Name; }
     const Persistent<v8::Context>& GetContext() const { return m_hContext; }
@@ -498,6 +502,8 @@ private:
 
     static void GetPromiseStateCallback(const v8::FunctionCallbackInfo<v8::Value>& info);
     static void GetPromiseResultCallback(const v8::FunctionCallbackInfo<v8::Value>& info);
+    static void WebSceneReadDomNumericPropertyCallback(const v8::FunctionCallbackInfo<v8::Value>& info);
+    static void WebSceneWriteDomRectCallback(const v8::FunctionCallbackInfo<v8::Value>& info);
 
     v8::MaybeLocal<v8::Promise> ImportModule(const V8DocumentInfo* pSourceDocumentInfo, v8::Local<v8::String> hSpecifier);
     v8::MaybeLocal<v8::Module> ResolveModule(v8::Local<v8::String> hSpecifier, const V8DocumentInfo* pSourceDocumentInfo);
diff --git a/ClearScriptV8/V8SplitProxyNative.cpp b/ClearScriptV8/V8SplitProxyNative.cpp
index 0297cbd7..6a03ee56 100644
--- a/ClearScriptV8/V8SplitProxyNative.cpp
+++ b/ClearScriptV8/V8SplitProxyNative.cpp
@@ -248,6 +248,13 @@ NATIVE_ENTRY_POINT(const StdChar*) V8SplitProxyNative_GetVersion() noexcept
 
 //-----------------------------------------------------------------------------
 
+NATIVE_ENTRY_POINT(StdBool) WebSceneTypedManagedAbi_Register(int32_t version, void* pvReadDomNumericProperty, void* pvWriteDomRect) noexcept
+{
+    return V8ContextImpl::RegisterWebSceneTypedManagedAbi(version, pvReadDomNumericProperty, pvWriteDomRect);
+}
+
+//-----------------------------------------------------------------------------
+
 NATIVE_ENTRY_POINT(void*) Memory_Allocate(size_t size) noexcept
 {
     return ::malloc(size);
diff --git a/ClearScriptV8/V8SplitProxyNative.h b/ClearScriptV8/V8SplitProxyNative.h
index 8ad6149e..608a84c3 100644
--- a/ClearScriptV8/V8SplitProxyNative.h
+++ b/ClearScriptV8/V8SplitProxyNative.h
@@ -169,6 +169,7 @@ DEFINE_V8_ENTITY_HANDLE(NativeCallbackHandle, HostObjectUtil::NativeCallback, SL
 
 NATIVE_ENTRY_POINT(void**) V8SplitProxyManaged_SetMethodTable(void** pMethodTable) noexcept;
 NATIVE_ENTRY_POINT(const StdChar*) V8SplitProxyNative_GetVersion() noexcept;
+NATIVE_ENTRY_POINT(StdBool) WebSceneTypedManagedAbi_Register(int32_t version, void* pvReadDomNumericProperty, void* pvWriteDomRect) noexcept;
 NATIVE_ENTRY_POINT(void) V8Environment_InitializeICU(const char* pICUData, uint32_t size) noexcept;
 
 NATIVE_ENTRY_POINT(void*) Memory_Allocate(size_t size) noexcept;
diff --git a/Unix/ClearScriptV8/Makefile b/Unix/ClearScriptV8/Makefile
index 6a27244f..01c0ff1d 100644
--- a/Unix/ClearScriptV8/Makefile
+++ b/Unix/ClearScriptV8/Makefile
@@ -163,7 +163,7 @@ clean:
 	rm -rf $(CLEARSCRIPTV8) $(OBJDIR)
 
 buildv8:
-	cd $(UNIXDIR); ./V8Update.sh -n -y $(CPU) $(CONFIG)
+	@test -f $(V8LIBDIR)/libv8_monolith.a || (echo "V8 monolith is missing; run Unix/V8Update.sh before make." >&2; exit 1)
 
 cleanv8:
 	rm -rf $(V8OUTDIR)
