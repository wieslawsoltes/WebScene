<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>net8.0;net10.0</TargetFrameworks>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <IsPackable>true</IsPackable>
    <Description>Optional ClearScript V8 adapter for WebScene JavaScript execution in Avalonia-native component hosts.</Description>
    <PackageId>JavaScript.Avalonia.ClearScript</PackageId>
    <PackageTags>avalonia;javascript;v8;clearscript;component-host</PackageTags>
    <RepositoryUrl>https://github.com/wieslawsoltes/WebScene</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageRequireLicenseAcceptance>false</PackageRequireLicenseAcceptance>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\WebScene.JavaScript\WebScene.JavaScript.csproj" />
  </ItemGroup>
  <ItemGroup>
    <!--
      Deliberately use the managed V8 package rather than ClearScript.Complete.
      Complete transitively supplies stock native binaries for every RID, which can
      silently bypass WebScene's reviewed same-origin-context patch. Applications must
      opt into an WebScene-reviewed native asset separately.
    -->
    <PackageReference Include="Microsoft.ClearScript.V8" />
  </ItemGroup>
  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
  </ItemGroup>
</Project>
