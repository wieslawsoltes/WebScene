# WebScene Hybrid React Islands

Run `npm install && npm run build` in `web`, then `dotnet run`. The checked-in
offline bundle makes packaging deterministic. Install the reviewed native V8 package
for your RID before executing the component.
